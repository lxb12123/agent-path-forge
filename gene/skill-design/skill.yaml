# gene/skill-design/skill.yaml
name: skill-design
description: Design an idea into a gene-compliant skill (process/methodology type, no deterministic scripts)
when-to-use: When you want to add a skill but haven't yet figured out which parts are deterministic, which parts to hand to the LLM, and how to evaluate it
version: "0.1.0"
# Process-type skill: no deterministic scripts; its value is the checklist + flow + red flags (a methodology-style skill)
uses:
  mcp: []
  permissions: []
  subagents: []
