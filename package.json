{
  "name": "agent-path-forge",
  "version": "0.1.0",
  "description": "An agent-native gene plugin: one idempotent command imprints a good-architecture gene into any project",
  "type": "module",
  "license": "MIT",
  "bin": { "agent-path-forge": "lib/cli.mjs" },
  "scripts": {
    "test": "node --test"
  },
  "engines": { "node": ">=18" },
  "files": ["lib", "gene", "commands", "plugin.json"]
}
